# Руководство по подключению реального AI API

## ✅ Текущее состояние

Сейчас AI работает в **улучшенном демо-режиме**, который дает **конкретные ответы** на вопросы. Демо-режим понимает контекст и отвечает развернуто, не переспрашивая.

## 🔧 Подключение реального AI API

### Вариант 1: OpenAI API (рекомендуется)

1. Зарегистрируйтесь на https://platform.openai.com
2. Получите API ключ на https://platform.openai.com/api-keys
3. Откройте файл `project/assets/js/ai-demo.js`
4. Найдите строки (около строки 320):
   ```javascript
   const apiKey = null;
   const apiUrl = null;
   ```
5. Замените на:
   ```javascript
   const apiKey = 'sk-ваш-ключ-openai';
   const apiUrl = 'https://api.openai.com/v1/chat/completions';
   ```

**Примечание:** OpenAI API платный, но дает очень качественные ответы.

### Вариант 2: HuggingFace API (бесплатный)

1. Зарегистрируйтесь на https://huggingface.co
2. Создайте токен на https://huggingface.co/settings/tokens
3. Откройте файл `project/assets/js/ai-demo.js`
4. Замените строки на:
   ```javascript
   const apiKey = 'hf_ваш-токен';
   const apiUrl = 'https://api-inference.huggingface.co/models/microsoft/DialoGPT-medium';
   ```

### Вариант 3: Anthropic Claude API

1. Зарегистрируйтесь на https://console.anthropic.com
2. Получите API ключ
3. Замените строки на:
   ```javascript
   const apiKey = 'sk-ant-ваш-ключ';
   const apiUrl = 'https://api.anthropic.com/v1/messages';
   ```

## 🎯 Настройка для конкретных ответов

В коде уже установлен параметр `temperature: 0.3` для более конкретных ответов (вместо 0.7). Это делает ответы более детерминированными и точными.

## 📝 Примеры вопросов для тестирования

- "Что такое JavaScript?"
- "Как создать веб-сайт?"
- "Объясни искусственный интеллект"
- "Расскажи о программировании"
- "Как работает CSS?"

## ⚠️ Важно

- Не публикуйте API ключи в публичных репозиториях
- Для продакшена используйте переменные окружения
- Демо-режим работает без API ключа и дает качественные ответы

